/*
  배열 : 여러 변수들이 같은 배열이름으로 일정한 크기의 연속된 크기의 연속된 메모리에 저장되는 구조
    배열 선언 방법 --> 원소 자료형 배열이름[배열크기];
    이때 배열의 크기를 지정하는 부분에는 양수 정수로 리터럴 상수와 매크로 상수 또는 이들의 연산식이 온다.
    배열선언 초기화 --> 원소 자료형 배열이름[배열크기] ={원소값1, 원소값2,...};
*/

/* 배열과 포인터 관계

*/
